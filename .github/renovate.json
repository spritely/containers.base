{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:best-practices"],
  "description": "Automated dependency updates with automerge",
  "automerge": true,
  "platformAutomerge": false,
  "prHourlyLimit": 0,
  "rebaseWhen": "behind-base-branch",
  "packageRules": [
    {
      "description": "Do not automerge spritely/actions.semantic-version or spritely/actions.publish-container (circular dependency: they use our devcontainer image)",
      "matchPackageNames": ["spritely/actions.semantic-version", "spritely/actions.publish-container"],
      "automerge": false,
      "groupName": "spritely actions"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update versions in shell scripts with renovate comments",
      "managerFilePatterns": [
        "/\\.sh$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+?) depName=(?<depName>[^\\s]+?)(?: extractVersion=(?<extractVersion>[^\\s]+?))?(?: versioning=(?<versioning>[^\\s]+?))?\\s+[A-Z_]+?=\"\\$\\{[A-Z_]+?:-(?<currentValue>.+?)\\}\""
      ]
    }
  ]
}
